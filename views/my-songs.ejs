<body class="bg-dark">
<%- include('partials/logged-in-header') -%>
<div class="mt-5 ">
<div class="container card-container ">
  <h1 class="text-light m-5 d-flex justify-content-center">My Songs</h1>
  <div class="row row-cols-1 row-cols-md-2 g-4">
    <% for(var i=0; i<posts.length; i++) {%>
    <div class="col">
      <div class="card h-100">
        <!-- <img src="<%= posts[i].image%>" class="card-img-top" alt="..."> -->
        <a class="text-dark card-body bg-danger" href="/post/<%= posts[i]._id%>">
          <h4 class="card-title"><b><%= posts[i].songName%></b></h4>
          <h5 class="card-text"><%= posts[i].bandName %></h5>
          
          <!-- <a href="/post/<%= posts[i]._id%>" class="btn btn-primary bg-dark">View Song</a> -->
          <!-- <a href="/post/<%= posts[i]._id%>" class="btn btn-primary">Edit</a> -->
        </a>
        
        <!-- <div class="card-footer">
          <span class="badge rounded-pill bg-primary"><%= posts[i].country%></span>
          <span class="badge rounded-pill bg-secondary"><%= posts[i].dish%></span>
        </div> -->
      </div>
    </div>
    <% } %>
  </div>
</div>
</div>

<%- include('partials/footer') -%>
</body>